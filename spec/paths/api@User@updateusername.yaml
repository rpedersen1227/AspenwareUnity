post:
  tags:
    - User
  operationId: UpdateUsername
  consumes:
    - application/json
  produces:
    - application/json
  parameters:
    - name: updateUsername
      in: body
      required: false
      schema:
        $ref: '#/definitions/UpdateCustomerUserAccount'
    - name: unity-api-version
      in: header
      description: The requested API version
      required: true
      type: string
      default: '3.0'
  responses:
    '200':
      description: Success
      schema:
        $ref: '#/definitions/UpdateCustomerUserAccountResponse'
    '401':
      description: Unauthorized
