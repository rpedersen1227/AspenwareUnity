post:
  tags:
    - Worker
  operationId: WorkerCheckUsername
  consumes:
    - application/json
  produces:
    - application/json
  parameters:
    - name: checkUsername
      in: body
      required: false
      schema:
        $ref: '#/definitions/WorkerCheckUsername'
    - name: unity-api-version
      in: header
      description: The requested API version
      required: true
      type: string
      default: '3.0'
  responses:
    '200':
      description: Success
      schema:
        $ref: '#/definitions/WorkerCheckResponse'
    '401':
      description: Unauthorized
