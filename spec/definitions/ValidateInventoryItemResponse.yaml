type: object
properties:
  inventoryItem:
    $ref: '#/definitions/InventoryItem'
  lockTag:
    type: string
  priceTag:
    type: string
  priceList:
    $ref: '#/definitions/PriceList'
  success:
    type: boolean
  message:
    type: string
