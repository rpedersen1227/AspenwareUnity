type: object
properties:
  requestLockId:
    format: int32
    type: integer
  lockId:
    format: int32
    type: integer
  isExpired:
    type: boolean
  activityLockData:
    $ref: '#/definitions/LockData'
