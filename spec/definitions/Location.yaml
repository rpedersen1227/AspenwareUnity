type: object
properties:
  locationId:
    $ref: '#/definitions/Identifier'
  parentLocationId:
    $ref: '#/definitions/Identifier'
  name:
    type: string
  description:
    type: string
  children:
    uniqueItems: false
    type: array
    items:
      $ref: '#/definitions/Location'
