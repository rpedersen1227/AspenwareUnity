type: object
properties:
  contactFirstName:
    type: string
  contactId:
    $ref: '#/definitions/Identifier'
  contactLastName:
    type: string
  entitlement:
    $ref: '#/definitions/Entitlement'
  entitlementId:
    $ref: '#/definitions/Identifier'
  notes:
    type: string
  resource:
    $ref: '#/definitions/Resource'
  resourceId:
    $ref: '#/definitions/Identifier'
  state:
    type: string
  uoM:
    enum:
      - Day
      - Hr
      - Min
      - Lesson
      - Item
    type: string
  updatedBy:
    $ref: '#/definitions/Identifier'
  usageDate:
    format: date-time
    type: string
  usageDetailId:
    $ref: '#/definitions/Identifier'
  usageId:
    $ref: '#/definitions/Identifier'
