type: object
properties:
  inventoryItem:
    $ref: '#/definitions/InventoryItem'
  lockTag:
    type: string
  priceTag:
    type: string
  confirmationId:
    type: string
  message:
    type: string
  success:
    type: boolean
  priceList:
    $ref: '#/definitions/PriceList'
