type: object
properties:
  sourceProductName:
    type: string
  productInventoryLocations:
    uniqueItems: false
    type: array
    items:
      $ref: '#/definitions/ProductInventoryLocation'
  inventoryDate:
    format: date-time
    type: string
  inventoryEndDate:
    format: date-time
    type: string
  quantity:
    format: int32
    type: integer
