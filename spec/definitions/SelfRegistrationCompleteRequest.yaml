type: object
properties:
  contactIds:
    uniqueItems: false
    type: array
    items:
      $ref: '#/definitions/Identifier'
  terminalClientCode:
    format: int32
    type: integer
  rentalEnabled:
    type: boolean
  minRentalUpdateDate:
    format: date-time
    type: string
  childRegistrationEnabled:
    type: boolean
  minChildRegUpdateDate:
    format: date-time
    type: string
  authTypeCode:
    format: int32
    type: integer
  minAuthProfileUpdateDate:
    format: date-time
    type: string
  salesChannelCode:
    format: int32
    type: integer
