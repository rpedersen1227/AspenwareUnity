description: Cancel Redemption
allOf:
  - $ref: '#/components/schemas/TimelineAction'
  - type: object
    properties:
      redemptionId:
        allOf:
          - $ref: '#/components/schemas/ResourceId'
