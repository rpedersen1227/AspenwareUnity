type: object
properties:
  voucherId:
    $ref: '#/components/schemas/Identifier'
  voucherDescription:
    type: string
  expirationDate:
    type: string
    format: date-time
  voucherText:
    type: string
