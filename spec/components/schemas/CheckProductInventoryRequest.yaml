type: object
properties:
  sourceProductName:
    type: string
  productInventoryLocations:
    uniqueItems: false
    type: array
    items:
      $ref: '#/components/schemas/ProductInventoryLocation'
  inventoryDate:
    type: string
    format: date-time
  inventoryEndDate:
    type: string
    format: date-time
  quantity:
    type: integer
    format: int32
