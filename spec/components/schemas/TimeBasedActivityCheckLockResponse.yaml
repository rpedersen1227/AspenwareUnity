type: object
properties:
  requestLockId:
    type: integer
    format: int32
  lockId:
    type: integer
    format: int32
  isExpired:
    type: boolean
  activityLockData:
    $ref: '#/components/schemas/LockData'
