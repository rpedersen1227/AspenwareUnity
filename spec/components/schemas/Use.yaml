type: object
properties:
  useId:
    $ref: '#/components/schemas/Identifier'
  resource:
    $ref: '#/components/schemas/Resource'
  resourceId:
    $ref: '#/components/schemas/Identifier'
  entitlement:
    $ref: '#/components/schemas/Entitlement'
  entitlementId:
    $ref: '#/components/schemas/Identifier'
  useDate:
    type: string
    format: date-time
  usageId:
    $ref: '#/components/schemas/Identifier'
  usageDetailId:
    $ref: '#/components/schemas/Identifier'
  points:
    type: integer
    format: int32
  updatedBy:
    $ref: '#/components/schemas/Identifier'
  isValid:
    type: boolean
