type: object
properties:
  inventoryItemId:
    $ref: '#/components/schemas/Identifier'
  locationId:
    $ref: '#/components/schemas/Identifier'
  productId:
    $ref: '#/components/schemas/Identifier'
  productAttributeValues:
    uniqueItems: false
    type: array
    items:
      $ref: '#/components/schemas/ProductAttributeValue'
  priceId:
    $ref: '#/components/schemas/Identifier'
  price:
    type: number
    format: double
  basePrice:
    type: number
    format: double
  saveAmount:
    type: number
    format: double
  savePercent:
    type: number
    format: double
