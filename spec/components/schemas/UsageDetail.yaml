type: object
properties:
  contactFirstName:
    type: string
  contactId:
    $ref: '#/components/schemas/Identifier'
  contactLastName:
    type: string
  entitlement:
    $ref: '#/components/schemas/Entitlement'
  entitlementId:
    $ref: '#/components/schemas/Identifier'
  notes:
    type: string
  resource:
    $ref: '#/components/schemas/Resource'
  resourceId:
    $ref: '#/components/schemas/Identifier'
  state:
    type: string
  uoM:
    type: string
    enum:
      - Day
      - Hr
      - Min
      - Lesson
      - Item
  updatedBy:
    $ref: '#/components/schemas/Identifier'
  usageDate:
    type: string
    format: date-time
  usageDetailId:
    $ref: '#/components/schemas/Identifier'
  usageId:
    $ref: '#/components/schemas/Identifier'
